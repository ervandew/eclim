# set the workspace
osgi.instance.area.default=@user.home/workspace.test

nailgun.server.port=9092

# increase heap space
-Xmx256M

# increase perm gen size
-XX:MaxPermSize=256m

# work around bug in hotspot 19
-XX:-UseCompressedOops
