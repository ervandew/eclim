# Defines classpath variables that can be used in .classpath files.
# System properties can be substituted in using ${...} references where any
# '.' characters in the property are replaced w/ '_'.
USER_HOME=${user_home}
